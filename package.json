{
  "name": "videofetch-backend",
  "version": "1.0.0",
  "description": "VideoFetch Pro Backend API",
  "main": "api/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "24.x"
  }
}
